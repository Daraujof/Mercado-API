version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mercado-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mercadodb
    ports:
      - "3306:3306"